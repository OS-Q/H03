version: 2
jobs:

  PIO:
    docker:
      - image: python:3
    steps:
      - checkout
      - run:
          name: "Install"
          command: |
            apt update
            apt install -y git
            git submodule update --init --recursive
            python -m pip install --upgrade pip
            pip install -U https://github.com/platformio/platformio/archive/develop.zip
            pio platform install https://github.com/OS-Q/P11/archive/pio.zip
      - run:
          name: "Build"
          command: |
            pio run -d examples/arduino-blink
            pio run -d examples/arduino-external-libs
            pio run -d examples/arduino-internal-libs
            pio run -d examples/arduino-own-src_dir
            pio run -d examples/simba-blink
            pio run -d examples/native-blink
            pio run -d examples/digitstump-mouse
            pio run -d examples/engduino-magnetometer
workflows:
    version: 2
    run_ci:
        jobs:
          - PIO
